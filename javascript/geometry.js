const area = (r) => Math.PI * r ** 2;
const circumfrence = (r) => 2 * Math.PI * r;
const eulerDouble = (r) => r * Math.E;


module.exports = {
	area: area,
	circumfrence: circumfrence,
	eulerDouble: eulerDouble
}
